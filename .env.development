# Development Environment Variables

# Prisma Database Configuration
DATABASE_URL="file:./prisma/dev.db"

# 🎭 Mock Auto-Update (pour tester l'UI sans vraie release)
# Décommenter la ligne ci-dessous pour activer :
MOCK_AUTO_UPDATE=true

# 🌐 Enable Real Auto-Update in Dev (teste contre GitHub releases)
# Décommenter la ligne ci-dessous pour tester avec de vraies releases :
# ENABLE_AUTO_UPDATE=true

# Note: Activer seulement l'une des deux options à la fois !
# - MOCK_AUTO_UPDATE = Simulation UI complète (recommandé)
# - ENABLE_AUTO_UPDATE = Vérifie vraiment sur GitHub (nécessite release)
